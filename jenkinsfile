pipeline {
    agent {label 'node1'}
    stages {
        stage ('git clone') {
            steps{
                git url: 'https://github.com/shashirajraja/onlinebookstore.git'
            }
        } 
        stage ('mvn compile'){
            steps{
                sh '''
                 cd workspace/maven-pipeline
                    mvn compile
                    '''
            }
        }
    }
}